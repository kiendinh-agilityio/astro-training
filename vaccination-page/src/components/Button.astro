---
type ButtonType = 'primary' | 'secondary';
export interface ButtonProps {
  type: ButtonType;
  className?: string;
  onclick: () => void;
}

const {
  className = '',
  onclick = () => { },
  type = 'primary',
} = Astro.props;
const theme = {
  primary: 'bg-primary',
  secondary: 'bg-secondary',
};
---

<button class={`${theme[type as ButtonType]} py-5 px-[25px] flex items-center
  text-sm leading-base font-bold ${className}`} {onclick}>
  <slot />
</button>
