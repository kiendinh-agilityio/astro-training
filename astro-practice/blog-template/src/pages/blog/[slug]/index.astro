---
import { BlogSkeleton } from '@/components';
import { MainLayout } from '@/layouts';
import { BlogDetail } from '@/sections';
import { getAllBlogPosts, getBlogPostBySlug } from '@/utils';

const slug = Astro.params.slug;
const posts = Astro.locals.cache?.blogPosts ?? getAllBlogPosts();
const post = slug ? getBlogPostBySlug(slug, posts) : null;
---

<MainLayout
  title={post?.title ?? 'Blog Template'}
  description={post?.introduction ?? 'Welcome to our blog'}
  url={Astro.url.href}
>
  {
    post ? (
      <BlogSkeleton client:load>
        <BlogDetail post={post} />
      </BlogSkeleton>
    ) : null
  }
</MainLayout>
